# APP_URL: The URL where this app is hosted.
APP_URL="http://localhost:3000"

# GEMINI_API_KEY: only needed if Gemini API calls are added (server-side only, never NEXT_PUBLIC_*).
GEMINI_API_KEY="MY_GEMINI_API_KEY"
